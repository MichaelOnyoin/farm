// Example: e:\HLB\farm\src\app\api\latest-no\route.ts
//import { NextRequest, NextResponse } from 'next/server'
// import your data reading logic here

// export async function GET(req: NextRequest) {
//   const { searchParams } = new URL(req.url)
//   const type = searchParams.get('type') || 'spray'
//   // Read your data (CSV, Google Sheet, etc.) and find the max no for the type
//   const maxNo = await getMaxNoForType(type) // implement this function
//   return NextResponse.json({ nextNo: maxNo + 1 })
// }


